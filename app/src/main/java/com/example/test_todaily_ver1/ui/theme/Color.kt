package com.example.test_todaily_ver1.ui.theme

import androidx.compose.ui.graphics.Color

// Primary Colors (보라색 계열)
val Purple500 = Color(0xFF615FFF)  // 피그마 Primary
val Purple700 = Color(0xFF4F39F6)  // 선택된 네비게이션
val PurpleDark = Color(0xFF9810FA) // 그라데이션 끝

// Background Colors
val BackgroundLight = Color(0xFFEFF6FF)  // 피그마 배경 시작
val BackgroundLight2 = Color(0xFFE0E7FF) // 피그마 배경 끝
val Surface = Color(0xFFFFFFFF)           // 카드 배경

// Text Colors
val TextPrimary = Color(0xFF312C85)      // 제목 (진한 보라)
val TextSecondary = Color(0xFF4A5565)    // 부제목
val TextTertiary = Color(0xFF717182)     // placeholder
val TextOnCard = Color(0xFF0A0A0A)       // 카드 제목
val TextMuted = Color(0xFF99A1AF)        // 빈 상태 텍스트

// Button Colors
val ButtonPrimary = Color(0xFF030213)    // 추가 버튼
val ButtonUnselected = Color(0xFFF3F3F5) // 미선택 버튼

// Priority Colors
val PriorityHigh = Color(0xFFEF4444)     // 빨강
val PriorityMedium = Color(0xFFFFA726)   // 주황
val PriorityLow = Color(0xFF66BB6A)      // 초록

// Dark Mode Colors
val DarkBackground = Color(0xFF1A1A1A)
val DarkSurface = Color(0xFF2D2D2D)
val DarkText = Color(0xFFE0E0E0)
