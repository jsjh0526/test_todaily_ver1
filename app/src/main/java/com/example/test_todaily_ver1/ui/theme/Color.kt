package com.example.test_todaily_ver1.ui.theme

import androidx.compose.ui.graphics.Color

// Primary Colors (보라색 - 피그마 정확)
val Purple500 = Color(0xFF615FFF)  // 우선순위 선택
val Purple600 = Color(0xFF4F39F6)  // 네비게이션 선택, 완료보기
val PurpleDark = Color(0xFF9810FA) // 그라데이션 끝

// Background (연보라 그라데이션)
val BackgroundLight = Color(0xFFEFF6FF)   // rgb(239, 246, 255)
val BackgroundLight2 = Color(0xFFE0E7FF)  // rgb(224, 231, 255)

// Text Colors
val TextTitle = Color(0xFF312C85)      // 제목 (할일 목록)
val TextSubtitle = Color(0xFF4A5565)   // 부제목
val TextPlaceholder = Color(0xFF99A1AF) // placeholder
val TextNavInactive = Color(0xFF6A7282) // 네비게이션 미선택
val TextOnCard = Color(0xFF0A0A0A)     // 카드 내용

// Button/Surface
val ButtonDark = Color(0xFF030213)     // 추가 버튼
val SurfaceGray = Color(0xFFF3F3F5)    // 검색창, 미선택 버튼

// Priority
val PriorityHigh = Color(0xFFEF4444)
val PriorityMedium = Color(0xFFFFA726)
val PriorityLow = Color(0xFF66BB6A)

// Dark Mode
val DarkBackground = Color(0xFF1A1A1A)
val DarkSurface = Color(0xFF2D2D2D)
val DarkText = Color(0xFFE0E0E0)
